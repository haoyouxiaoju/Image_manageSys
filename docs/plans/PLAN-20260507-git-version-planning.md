# PLAN-20260507-git-version-planning

## 1. 任务背景

需要按既定规程启动推进，并明确 Git 版本规划，重点解决“前后端内容要区分提交”的执行规则。

## 2. 目标与验收结果

- 目标 1：形成一份可执行的 Git 版本与提交流程文档。
- 目标 2：明确前后端分离提交规则（分支、提交、PR、Tag）。
- 目标 3：将文档入口加入 README，作为后续执行基线。

验收标准（必须可验证）：
- [x] 新增 Git 规划文档，包含前后端分离提交规则。
- [x] 给出版本号与 Tag 规划（前端、后端、系统集成）。
- [x] README 增加文档入口。

## 3. 本次范围 / 非范围

### 3.1 In Scope
- 文档化 Git 分支策略、提交规范、PR 规则、Tag 规则。
- 文档化前后端拆分提交的硬性约束。

### 3.2 Out of Scope
- 不涉及代码功能实现。
- 不配置远端仓库保护规则（仅给出执行方案）。

## 4. 技术方案

- 在 `docs/` 下新增 Git 规划文档。
- 在 `README.md` 的文档入口处增加链接。
- 使用既有规程（Plan 先行）作为执行约束。

## 5. 任务拆解与推进顺序

1. 产出本次 Plan 文档并锁定验收标准。
2. 编写 Git 版本规划与前后端提交流程文档。
3. 更新 README 文档入口。
4. 自检文档完整性并交付。

## 6. 风险与回滚

- 风险点：规则不够具体，执行时出现歧义。
- 监控点：提交记录是否出现前后端混杂提交。
- 回滚方案：仅文档变更，可直接按文件回退。

## 7. 验证方案

- 检查文档是否覆盖：分支、提交、PR、Tag、版本演进。
- 检查 README 是否存在新文档链接。

## 8. 交付清单

- 代码文件：无
- 文档文件：
  - `docs/plans/PLAN-20260507-git-version-planning.md`
  - `docs/Git版本规划与前后端提交流程.md`
  - `README.md`（文档入口更新）
- 变更说明：已形成可直接执行的 Git 规划基线。
