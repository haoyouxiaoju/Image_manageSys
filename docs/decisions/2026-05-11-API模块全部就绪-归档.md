# 决策归档：API 模块全部就绪

> **日期**：2026-05-11
> **归档来源**：`docs/前后端对接-待确认问题.md`
> **背景**：后端 7 个 API 模块（17 个端点）已全部实现并就绪，前端可随时切换 mock 逻辑为真实 API 调用。

---

## 归档内容概览

| 模块 | 端点数 | 状态 | 对应 Mock 位置 |
|------|--------|------|---------------|
| 1. 素材管理 (assets) | 2 | 🟢 已就绪 | `store.addVersion()` |
| 2. 标签管理 (tags) | 3 | 🟢 已就绪 | `stores/assets.ts` mockTags |
| 3. 分组管理 (collections) | 7 | 🟢 已就绪 | `stores/assets.ts` mockCollections |
| 4. 分享管理 (share-links) | 3 | 🟢 已就绪 | ShareManageView 本地 ref |
| 5. 审计日志 (audit) | 1 | 🟢 已就绪 | AuditView.vue 本地数组 |
| 6. 文本搜索 (search) | 1 | 🟢 已就绪 | `store.doSearch()` 本地模拟 |
| 7. 图像关键词分析 | 2 | 🟢 已就绪 | UploadView.vue setTimeout 模拟 |

---

## 1. 素材管理（assets）— P0

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 1 | `POST` | `/api/v1/assets/{id}/versions` | 上传新版本 | `store.addVersion()` mock |
| 2 | `GET` | `/api/v1/assets/{id}/versions` | 版本历史 | 素材详情中的 versions 字段 |

**对接要点**：
- `store.addVersion()` 当前为本地数组操作，需替换为 `POST /api/v1/assets/{id}/versions`（FormData）
- 版本列表需从 API 获取而非本地空数组

---

## 2. 标签管理（tags）— P0

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 3 | `GET` | `/api/v1/tags` | 标签列表（含 asset_count） | `stores/assets.ts` mockTags 本地数组 |
| 4 | `POST` | `/api/v1/tags` | 创建标签 | 前端 allow-create 自动追加 |
| 5 | `DELETE` | `/api/v1/tags/{id}` | 删除标签 | 无（标签为素材内联数组） |

**对接要点**：
- `allTags` ref 初始化为本地 `mockTags`，需替换为 `GET /api/v1/tags` 加载
- 标签以字符串数组内联在 Asset 中（`tags: string[]`）

---

## 3. 分组管理（collections）— P0

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 6 | `GET` | `/api/v1/collections` | 分组列表 | `stores/assets.ts` mockCollections |
| 7 | `POST` | `/api/v1/collections` | 创建分组 | `store.createCollection()` 本地追加 |
| 8 | `GET` | `/api/v1/collections/{id}` | 分组详情（含素材列表） | `store.getCollectionById()` 本地查找 |
| 9 | `PUT` | `/api/v1/collections/{id}` | 编辑分组 | 暂无（前端未实现编辑分组信息） |
| 10 | `DELETE` | `/api/v1/collections/{id}` | 删除分组 | `store.deleteCollection()` 本地 splice |
| 11 | `POST` | `/api/v1/collections/{id}/assets` | 向分组添加素材 | `store.addToCollection()` 本地 push |
| 12 | `DELETE` | `/api/v1/collections/{id}/assets/{asset_id}` | 从分组移除素材 | `store.removeFromCollection()` 本地 filter |

---

## 4. 分享管理（share-links）— P0

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 13 | `GET` | `/api/v1/share-links` | 分享链接列表 | ShareManageView 本地 shareLinks ref |
| 14 | `POST` | `/api/v1/share-links` | 创建分享链接 | confirmCreateLink() 本地 unshift |
| 15 | `DELETE` | `/api/v1/share-links/{id}` | 撤销分享链接 | revokeLink() 本地改 isActive |

**对接要点**：
- 创建请求：`{ asset_id: number, expires_in_hours: number }`
- 响应需包含：id, token, url, asset_name, created_at, expires_at, is_active

---

## 5. 审计日志（audit）— P0

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 16 | `GET` | `/api/v1/audit-logs` | 审计日志列表（支持筛选） | AuditView.vue 本地 auditLogs 数组 |

**对接要点**：
- 支持筛选参数：`user_id`、`action`、`start_date`、`end_date`
- 响应字段：id, user, action, target, ip_address, created_at

---

## 6. 文本搜索（search）

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 17 | `POST` | `/api/v1/search/text` | 文本搜图 `{ query, page, page_size }` | `store.doSearch()` 本地语义模拟 |

**后端实现**：
- `POST /api/v1/search/text` 端点
- 基于图片分析结果中的 `prompt` 做向量召回（Qdrant）
- `clip_repository.list_ready_embeddings_assets()` 读取可检索素材分析数据
- 向量检索排序 + 分页 `{ items: [{ asset, score }], total, page, page_size }`
- 测试覆盖检索分页与排序

---

## 7. 图像关键词分析（Qwen3-VL）

| # | 方法 | 路径 | 说明 | 前端当前占位 |
|---|------|------|------|-------------|
| 18 | `GET` | `/api/v1/clip/status` | 模型状态 | UploadView 未调用 |
| 19 | `POST` | `/api/v1/clip/analyze` | 图生文分析 | UploadView.vue setTimeout 模拟 |

**后端响应格式**（`clip.py:17-24`）：
```json
{
  "provider": "qwen3_vl",
  "model": "qwen3-vl-plus",
  "model_version": "1.0",
  "prompt": "一本打开的书放在木质桌面上...",
  "summary": "一本打开的书放在木质桌面上，旁边有笔记本。",
  "keywords": ["书籍", "书桌", "阅读", "笔记本", "办公"]
}
```

**2026-05-11 已完成适配**：
- `UploadView.vue` 分析结果展示改为 prompt/summary/keywords
- `clip.ts` 类型定义对齐后端实际返回
- 前端文案统一从 CLIP 改为 Qwen-VL / AI 图片分析

---

## 前端切换计划已完成项

| 任务 | 涉及文件 | 状态 |
|------|---------|------|
| CLIP 分析接入真实 API | `UploadView.vue` | ✅ commit 78524d7 |
| 搜索接入真实 API | `SearchView.vue` / `stores/assets.ts` | ✅ 已实现 |
| Qwen-VL 适配（类型+文案） | `clip.ts` / `UploadView.vue` / `SearchView.vue` | ✅ 2026-05-11 |
